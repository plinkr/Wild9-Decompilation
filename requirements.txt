# Python tooling used by the Wild 9 decompilation project.
# Keep the versions fixed for reproducible analysis/build tooling.
splat64[mips]==0.50.0
spimdisasm==1.42.4

# asm-differ runtime dependencies
colorama==0.4.6
watchdog==6.0.0
levenshtein==0.27.1
cxxfilt==0.3.0
